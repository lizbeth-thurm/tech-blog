<section class="card">

    <div class="card-body">

        <div class="card-title">
            <p>Title: {{title}}</p>
        </div>

        <div class="card-content">
            <p>{{text}}</p>
        </div>

    </div>